# 本地开发环境配置示例
# 复制此文件为 application-dev.yml 并根据你的本地环境修改
# 或者直接使用环境变量覆盖默认值

# 如果本地使用 Docker Compose 启动中间件，可以使用以下配置：
# spring:
#   datasource:
#     url: jdbc:mysql://localhost:3306/admin?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
#     username: admin
#     password: admin
#   data:
#     redis:
#       host: localhost
#       port: 6379
#   elasticsearch:
#     uris: http://localhost:9200
# nacos:
#   server-addr: localhost:8848
# rocketmq:
#   endpoints: localhost:18080
# management:
#   otlp:
#     tracing:
#       export:
#         endpoint: http://localhost:4318/v1/traces

# 注意：application.yml 中已经设置了默认值（localhost），
# 所以本地开发时通常不需要创建此文件，除非需要覆盖某些配置。

