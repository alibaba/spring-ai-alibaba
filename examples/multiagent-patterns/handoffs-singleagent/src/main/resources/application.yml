spring:
  application:
    name: handoffs-singleagent
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY:}

# Set to true to run the four-turn support workflow demo on startup (single-agent state machine).
handoffs.run-examples: false
