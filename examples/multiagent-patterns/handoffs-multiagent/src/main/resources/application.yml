spring:
  application:
    name: handoffs-multiagent
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY:}

# Set to true to run the multi-agent handoffs demo on startup (sales + support with handoff tools).
handoffs.runner.enabled: true
